/**
 * @file ./src/components/chat/memoryTagExtraction.ts
 * @description Stub for T15 — memory tag extraction is ported in a later task.
 */

export interface MemoryTags {
  userSoul?: string;
  userMemory?: string;
  convMemory?: string;
}

export function extractMemoryTags(_text: string): MemoryTags {
  // T15 — return empty until memory extraction is ported
  return {};
}
